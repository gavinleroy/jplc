(library
 (name interping)
 (libraries
   ;; libraries for using MetaOCaml
   dynlink metaocaml

   typing runtime fmt)
  (flags (:standard -w -27 -w -32)) ;; TODO remove me
 (preprocess (pps ppx_jane)))
